```javascript
// 01
var str = '';
for (var i = 0; i < 10; i++) {
    // var str = ''; // 여기서 변수선언하면 돌때마다 재할당되서 초기화 될거야
    if (!(i % 2)) { // 그룹연산자와 부정연산자를 사용해야하므로 i % 2 === 0이 가독성이 더 좋다.        
        console.log(i); 
    }
}
```

